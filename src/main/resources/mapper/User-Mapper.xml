<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.booktory.booktoryserver.users.mapper.UserMapper">

    <insert id="insertUser" parameterType="user" useGeneratedKeys="true" keyProperty="user_id">
        INSERT INTO users (user_email, user_password, user_nickname, user_mobile, user_gender, user_birth, user_address, user_role)
        VALUES (#{user_email}, #{user_password}, #{user_nickname}, #{user_mobile}, #{user_gender}, #{user_birth}, #{user_address}, #{user_role})
    </insert>

    <select id="findByEmail" resultType="user" parameterType="String">
        SELECT * FROM users WHERE user_email = #{user_email}
    </select>

</mapper>
